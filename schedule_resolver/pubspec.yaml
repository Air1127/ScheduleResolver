name: schedule_resolver
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # Icons and Fonts
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2

  # State Management and Logic
  provider: ^6.1.5+1
  uuid: ^4.5.3      # Removed the duplicate entry and kept the latest version
  intl: ^0.20.2

  # AI Integration
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Fixed: Updated to a stable version compatible with SDK 3.10.x
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true